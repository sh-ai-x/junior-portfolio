<!-- AUTO-GENERATED bypass of /dev-kit:plan manual session flow -->
<!-- All 5 interview fields locked via interview-feat-p1-ai-memo-build.md (status: ok) -->
<!-- Phase shape mirrors SOT §implementation phases. Date: 2026-08-11 -->

# PRD — 01-ai-memo build (feat/p1-ai-memo-build)

## 1. Frame

- goal: Ship 01-ai-memo as a working Next.js 15 + pgvector + GPT-4o-mini demo on Vercel — write memo, auto-tag + summarize via LLM, semantic search via pgvector, public URL responsive on desktop and mobile.
- target_user: Hiring manager / senior backend engineer evaluating junior candidates for backend + AI roles at Korean AI-integrated companies.
- situation: Today the evaluator sees only the project README. The deployed URL + semantic-search demo is the strongest single signal of "depth + ship" combined.

## 2. Validate

- interview contract: `./.dev-kit/hand-off/interview-feat-p1-ai-memo-build.md` — status ok, ambiguity_score 2 (≤ 3).
- SOT: `../../.dev-kit/hand-off/sot-harness-p1-ai-memo.md` — R1 (domain-first hexagonal + TDD) and R2 (A exit codes + B property-based + C port contracts) locked.
- value: portfolio piece for 26 employer signal ÷ ~$10 OpenAI + Vercel free + Neon free + 8 weeks dev = strong positive.

## 3. Non-Goals

| # | Non-goal | Rationale | Breach-response |
|---|---|---|---|
| A | No Clerk auth swap | README commits NextAuth; swap is feature creep | defer to v2 — NextAuth sufficient, Clerk adds SDK complexity |
| B | No mobile push / native sync | Web-only demo; mobile sync adds API surface | defer to v2 — web responsiveness covers demo |
| C | No public OSS release | Demo not reusable library; OSS would dilute portfolio framing | defer to v2 — first prove scale, then consider |

## 4. Phase plan

Source: `./.dev-kit/phases/p1-ai-memo-build/index.json`

| Step | Title | Status |
|---|---|---|
| 1 | Domain core — Memo, Tag, Embedding types + pure functions + property tests | pending |
| 2 | Ports — TaggerPort, EmbedderPort, MemoRepositoryPort + contract tests | pending |
| 3 | Adapters — OpenAITagger, OpenAIEmbedder, pgvector repo (real impls) | pending |
| 4 | HTTP layer — Next.js App Router + RSC + NextAuth wiring | pending |
| 5 | UI — Memo list, editor, semantic search page | pending |
| 6 | Deploy — Vercel env config + production smoke test | pending |

## 5. Acceptance Criteria (1:1 to step AC)

| AC | Step | Pass condition |
|---|---|---|
| AC1 | 1 | vitest exit 0; ≥12 property-based tests on Memo/Tag invariants pass |
| AC2 | 2 | FakeTagger + OpenAITagger both pass `test_<port>_contract.ts` |
| AC3 | 3 | pgvector testcontainer integration test passes |
| AC4 | 4 | tsc --noEmit exit 0; next build exit 0; routes return 200 on dev smoke |
| AC5 | 5 | Playwright smoke on memo list / editor / search passes; Lighthouse ≥ 90 mobile |
| AC6 | 6 | Production URL responds < 500ms p95; demo flow works end-to-end |

## 6. Hand-off

- next stage: /dev-kit:build (per-step worktree + harness-runner execution)
- Iron Laws in scope: L1 (TDD), L3 (evidence), L4 (regression), L5 (worktree-isolated)
- Operating env: sh-ai-x/junior-portfolio with MINIMAX_API_KEY + DEV_KIT_GITHUB_TOKEN configured
